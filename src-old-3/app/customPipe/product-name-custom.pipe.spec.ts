import { ProductNameCustomPipe } from './product-name-custom.pipe';

describe('ProductNameCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductNameCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
